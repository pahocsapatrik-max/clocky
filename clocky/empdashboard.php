<?php session_start(); /* ===== BEJELENTKEZÉS ÉS ROLE ELLENŐRZÉSE ===== */ $role = (int)($_SESSION['role'] ?? -1); if (!isset($_SESSION['logged_in'], $_SESSION['role']) || $_SESSION['logged_in'] !== true || $role !== 0) { header('Location: index.php'); exit; } /* ===== LOGOUT KEZELÉS ===== */ if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['logout'])) { session_destroy(); header('Location: index.php'); exit; } ?> <!DOCTYPE html> <html lang="hu"> <head> <meta charset="UTF-8"> <title>Felhasználói Dashboard</title> <style> body {font-family:Arial,sans-serif; background:#f4f4f4; height:100vh; display:flex; justify-content:center; align-items:center;} .box {background:white; padding:30px; border-radius:12px; text-align:center; box-shadow:0 10px 20px rgba(0,0,0,0.15);} button {margin-top:25px; padding:12px 20px; border:none; border-radius:8px; font-weight:bold; cursor:pointer; background:#c0392b; color:white;} button:hover {background:#e74c3c;} </style> </head> <body> <div class="box"> <h1>Szia <?php echo htmlspecialchars($_SESSION['name'] ?? 'Felhasználó'); ?> 👋</h1> <p>Üdvözlünk a felhasználói felületen!</p> <form method="post"> <button type="submit" name="logout">Kijelentkezés</button> </form> </div> </body> </html>